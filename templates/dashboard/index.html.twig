{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row g-4">
            <div class="col-12">
                <div class="card p-4">
                    <h1 class="page-title-heading h3 mb-2">
                        {{ pageIconHtml|raw }}
                        <span>{{ pageTitle }}</span>
                    </h1>
                    <p class="text-muted mb-0">La connexion et la reinitialisation du mot de passe sont actives. Tu peux maintenant acceder aux differents modules depuis le menu lateral.</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
